name: Mobile Automation Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-android:
    name: Testes Android - BrowserStack
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4 # Atualizado de v3 para v4

      - name: Instalar Node.js
        uses: actions/setup-node@v4 # Atualizado de v3 para v4
        with:
          node-version: '22'

      - name: Instalar dependências
        run: npm ci

      - name: Rodar testes no BrowserStack
        env:
          BROWSERSTACK_USER: ${{ secrets.BROWSERSTACK_USER }}
          BROWSERSTACK_KEY: ${{ secrets.BROWSERSTACK_KEY }}
          BROWSERSTACK_APP_URL: ${{ secrets.BROWSERSTACK_APP_URL }}
        run: npm run test:browserstack

      - name: Gerar relatório Allure
        if: always()
        run: npx allure generate allure-results --clean -o allure-report

      - name: Salvar relatório como artefato
        if: always()
        uses: actions/upload-artifact@v4 # Atualizado para v4 (Corrige o erro de deprecation)
        with:
          name: allure-report-android
          path: allure-report/

      - name: Salvar screenshots como artefato
        if: always()
        uses: actions/upload-artifact@v4 # Atualizado para v4
        with:
          name: screenshots-android
          path: test/screenshots/